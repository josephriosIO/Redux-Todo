const intitalState = {
  todos: "get food!",
  completed: false
};

const reducer = (state = intitalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
